# GitHub Pages deployment workflow - DISABLED
#   The React app files are now in the root directory and served directly from main branch
#   push:
#     branches: [ main ]
# 
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     
#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4
#       
#     - name: Setup Node.js
#       uses: actions/setup-node@v4
#       with:
#         node-version: '18'
#         
#     - name: Install dependencies
#       run: |
#         cd frontend
#         npm install --legacy-peer-deps
#         
#     - name: Build
#       run: |
#         cd frontend
#         npm run build
#         
#     - name: Copy files to root
#       run: |
#         cp -r frontend/dist/* .
#         
#     - name: Deploy to GitHub Pages
#       uses: peaceiris/actions-gh-pages@v3
#       with:
#         github_token: ${{ secrets.GITHUB_TOKEN }}
#         publish_dir: ./
#         publish_branch: gh-pages
